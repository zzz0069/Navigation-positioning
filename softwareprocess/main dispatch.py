#import dispatch as dispatch
#a = dispatch.dispatch({'op': 'predict'})
#print a
import random
import sys
import os

class myName:
    def name1(self,name):
        self.name = name
        return self.name

that = myName()
print that.name1('tony')
